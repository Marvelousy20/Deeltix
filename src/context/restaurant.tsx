"use client";
import { RestaurantDetails } from "@/types";
import React, { createContext } from "react";

export const RestaurantContext = () => {
  const restaurant = createContext(null);
  return <div>restaurant</div>;
};
